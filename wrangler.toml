# Static site for Cloudflare Workers (Git deploy runs `npx wrangler deploy`).
# Do not set `main` — there is no Worker script, only HTML/CSS/JS/images.
name = "anshika-mahesh"
compatibility_date = "2026-08-14"

[assets]
directory = "./anshika-mahesh-site"
